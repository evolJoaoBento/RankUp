name: CI

on:
  push:
    branches: [main, improvements]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    services:
      db:
        image: postgres:16
        env:
          POSTGRES_USER: mecateca
          POSTGRES_PASSWORD: mecateca
          POSTGRES_DB: mecateca
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U mecateca"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: pip install -e ".[dev]"
      - name: Ruff (lint)
        run: ruff check src
      - name: Tests
        run: pytest -q
