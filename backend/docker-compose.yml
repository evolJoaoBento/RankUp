services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: mecateca
      POSTGRES_PASSWORD: mecateca
      POSTGRES_DB: mecateca
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mecateca"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
